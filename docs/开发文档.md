# PacDown 开发文档

> 面向二次开发与维护者 · v1.1 · 2026-08

## 1. 本地开发环境

```bash
git clone <repo> && cd pac
python -m venv .venv
.venv\Scripts\activate          # Windows
pip install -r requirements.txt
python run.py                   # http://127.0.0.1:8300
```

可选：安装 ffmpeg（MP3 提取与视频工具需要）、Node 无需（前端无构建）。

## 2. 目录结构

```
pac/
├── run.py                  # 入口（uvicorn 启动）
├── start.bat               # Windows 一键启动（自动 venv）
├── requirements.txt
├── config.json             # 运行时生成（gitignore）
├── Dockerfile / docker-compose.yml
├── app/
│   ├── main.py             # FastAPI 全部路由
│   ├── config.py           # 配置：默认值/读写/目录切换/敏感字段
│   ├── database.py         # sqlite3 直连（线程锁），videos/subscriptions/reposts/notifications/tool_jobs
│   ├── downloader.py       # DownloadManager：worker 线程池、原子认领、进度、后处理编排
│   ├── postprocess.py      # ffmpeg MP3、B站弹幕 XML、截帧
│   ├── scheduler.py        # APScheduler + wbi 签名 + 四平台博主列表
│   ├── toolbox.py          # 工具箱：ffmpeg/Pillow 任务执行 + 上传
│   ├── ai.py               # AI 文案改写（OpenAI 兼容）
│   └── parsers/
│       ├── base.py         # VideoInfo / Parser / @register / dispatch / URL 提取
│       ├── ytdlp_engine.py # yt-dlp 封装（probe/download/cookie/清晰度/回退/Referer）
│       ├── http_download.py# httpx 流式下载（断点续传）/短链/内嵌 JSON 提取/Referer 表
│       ├── bilibili.py douyin.py kuaishou.py xiaohongshu.py direct.py generic.py
├── static/
│   ├── index.html          # 页面骨架（6 页 + 弹窗 + 灯箱 + 通知抽屉）
│   ├── css/design.css      # 设计系统（CSS 变量双主题）
│   └── js/app.js           # 全部交互逻辑（无框架）
├── downloads/              # 输出（平台/作者/模板命名.*）；_tools/ 为工具产物
├── data/metadata.db        # uploads/ 为工具箱上传素材
└── docs/                   # 本文档所在
```

## 3. 关键机制

### 3.1 任务与历史合一
`videos` 表既是下载任务也是历史记录：`status` 即任务状态机
（parsing → working → pending → downloading → processing → done | failed）。
活动任务查询：`status IN ('pending','parsing','working','downloading','processing')`；
前端 1.5s 轮询 `/api/tasks`。

### 3.2 原子认领（防重复执行）
worker 取任务用 `database.claim_task(from, to)`：`UPDATE … WHERE status=?` 的
rowcount 决定是否认领成功。v1.0 的 SELECT-then-run 在多 worker 下会重复执行同一
任务（解析慢时必现）。认领状态 `working` 是解析中的占位态，UI 显示同"解析中"。

### 3.3 进度写库节流
progress hook 每块回调，直接写库会打爆 SQLite。`downloader._run` 中
`last["t"]` 节流 0.4s；yt-dlp 路径 `_percent_str/_speed_str`，直链路径按
字节数计算；ffmpeg 工具任务解析 `-progress pipe:1` 的 out_time。

### 3.4 线程安全
- SQLite：单连接 `check_same_thread=False` + 全局 `threading.Lock`
- 配置：`config.py` 读写均持锁
- worker 线程通过数据库轮询 + Event 唤醒取任务（`_wake.set()`）

### 3.5 Cookie 流转
- 配置存 `config.json`（明文本地）
- yt-dlp 用法：`_cookie_file()` 生成 Netscape 格式临时文件，用完删除
- httpx 用法：直接塞 `Cookie` 请求头
- API 回显打码：`__SET__`（已配置）/ 前端提交 `__KEEP__`（保持不变）

### 3.6 封面防盗链
平台封面 URL 有 Referer 校验。前端 `<img>` 一律走
`/api/cover?url=...&platform=...` 代理，后端按平台附加 Referer 拉取。
无平台封面的记录（直链等）下载后用 ffmpeg 截帧生成本地封面，
走 `/api/file?type=cover` 输出。

### 3.7 命名模板
`config.name_template`（默认 `{date}_{title}`）在 `downloader.render_name`
渲染，变量：`{date} {title} {author} {platform} {id}`。所有解析器的
`download()` 都必须接受 `filename_prefix` 参数并优先使用它
（v1.0 曾因此导致 kuaishou/xiaohongshu 下载必崩——签名漏参）。

### 3.8 直链锚点
`url#direct;name=xx.mp4;referer=...`：`#direct` 强制直链通道，
`name` 指定文件名，`referer` 绕防盗链。注意 URL 提取正则（base.py URL_RE）
会排除半角 `!` 等标点，锚点参数用 `;` 分隔、`key=value` 形式。

### 3.9 前端 hidden 约定
组件普遍用 `hidden` 属性控制显隐，CSS 里若给组件写了 `display:flex/grid`
会覆盖 hidden——全局 `[hidden]{display:none!important}` 兜底，新组件依赖此规则。

## 4. 新增平台解析器（贡献指南）

1. 新建 `app/parsers/yourplatform.py`：

```python
from . import http_download as hd
from .base import ParseError, Parser, VideoInfo, register

@register
class YourParser(Parser):
    platform = "yourplatform"
    display_name = "平台名"

    def can_parse(self, url): 
        return "yourplatform.com" in url.lower()

    def parse(self, url) -> VideoInfo:
        # 解析元数据，失败 raise ParseError("人话错误信息")
        return VideoInfo(platform=self.platform, ...)

    def download(self, info, dest_dir, options, progress, filename_prefix=""):
        # progress(0~100, "速度描述")；返回 {file_path, file_size, images?}
        # filename_prefix 优先作为文件名主体（命名模板产物）
        return hd.stream_download(direct_url, dest, "yourplatform", progress, referer=...)
```

2. `app/parsers/__init__.py`：import 注册 + `PLATFORM_META` 加名称与品牌色
3. `app/parsers/base.py`：`KNOWN_HOSTS` 加域名（用于链接识别提示）
4. 前端 `app.js`：`PLATFORM_NAME` 加显示名；`index.html` 历史筛选加 option
5. CSS 徽章色：`design.css` 加 `.badge-yourplatform`

解析失败信息要面向用户（说明原因和补救办法），模块间互不影响。

## 5. 调试技巧

```bash
# 单测某个解析器
python -c "
from app.parsers import dispatch
info = dispatch('https://...').parse('https://...')
print(info.title, info.cover_url, info.raw.get('direct_url'))"

# 测试实例（避开用户手动启动的 8300 端口！）
python -c "import uvicorn; uvicorn.run('app.main:app', port=8301)"

# 看 worker 实时日志（任务表）
curl -s http://127.0.0.1:8300/api/tasks | python -m json.tool

# 直接查库
sqlite3 data/metadata.db "SELECT id,status,progress,title,error FROM videos ORDER BY id DESC LIMIT 5"

# UI 冒烟（Playwright，含截图与 console 收集）
python scripts/smoke_ui.py
```

常见坑：
- **平台接口又变了**：优先在该平台 parser 内修，勿动公共层；错误消息测试给用户看
- **yt-dlp 解析挂了**：`pip install -U yt-dlp` 通常能修（B站/YouTube 变化它更新最快）
- **412/风控**：降低请求频率；引导配置 Cookie；B站空间有 wbi+yt-dlp 双路径自动回退
- **Windows 控制台中文乱码**：`chcp 65001`
- **Windows 下载完成改名报 WinError 32**：杀毒/索引短暂占用新文件，`stream_download` 已带重试
- **测试后务必释放 8300 端口**：用户手动启动会撞 WinError 10048

## 6. 构建与发布

```bash
# Docker
docker compose up -d --build

# 版本升级依赖
pip install -U yt-dlp && pip freeze | grep -i ytdlp
```

版本发布前检查清单：
- [ ] B站公开视频解析+下载通过；多分P视频出现「展开批量下载」
- [ ] 抖音（配置 Cookie）解析通过；合集页链接列出视频列表
- [ ] 直链下载（断点续传）通过；命名模板生效
- [ ] 片库标签/收藏/批量删除/列表视图正常
- [ ] 工具箱截帧与图片转换通过（ffmpeg/Pillow）
- [ ] 历史统计数字正确、CSV 导出打开不乱码
- [ ] 明暗主题切换正常、封面代理可用
- [ ] ffmpeg 缺失时 MP3 选项与视频工具给出明确报错

## 7. 已知限制

| 限制 | 原因 | 计划 |
|---|---|---|
| 抖音免 Cookie 解析不可用 | 平台 2025 年关闭公开通道 | Cookie 路径已实现；后续跟进社区新通道 |
| 小红书解析依赖 Cookie | 强风控 | 保持解析器隔离，接口恢复即可用 |
| 小红书/快手订阅为实验性 | 主页结构多变 | 失败会写入订阅卡片错误 |
| B站番剧/课程部分需要会员 | 权限限制 | 配置会员 Cookie 可解 |
| 订阅抖音博主成功率一般 | 主页风控 | 建议 Cookie；错误已在卡片展示 |
| 网盘分享页不直接解析 | 无免登录通道 | 直链插件提取后粘贴下载 |

